////////////////// Model Template Extended/ /////////////////////////////


var MyModel = Backbone.Model.extend();

var artistModel = Backbone.MyModel.extend({
    
}
        );

var musicianModel = Backbone.MyModel.extend({
    
}
        );

var eventModel = Backbone.MyModel.extend({
    
}
        );
