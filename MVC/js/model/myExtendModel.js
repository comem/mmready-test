////////////////// Model Template Extended/ /////////////////////////////



var artistModel = MyModel.extend({
    
}
        );

var musicianModel = MyModel.extend({
    defaults: function() {
            return {
                createdAt: $.now()
            };
        }
}
        );


